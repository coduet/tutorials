# tuples are collections of elements
# Major difference between list and tuples is that tuples are immutable.
# tuples used mostly for data that doesn't change
################################

coordinates = (1.2,2.1,2.21)

print(coordinates)
print(coordinates[1])
